# Deploy on Render: https://render.com — New → Blueprint → paste this repo URL.
services:
  - type: web
    name: stadiumiq
    runtime: node
    plan: free
    buildCommand: npm install --prefix server
    startCommand: node server/index.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20"
